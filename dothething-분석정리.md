# dothething (DTT) 완전 분석 정리

> 이 문서는 `dothething` 저장소를 직접 뜯어보며 나눈 대화를 정리한 것입니다.
> 설치법부터 수익화 전략까지, 개발자가 아니어도 이해할 수 있게 썼습니다.

---

## 🔗 관련 주소 모음

| 구분 | 주소 |
|---|---|
| **이 저장소 (포크)** | https://github.com/bmshin94/dothething |
| **원본 저장소** | https://github.com/fluffypony/dothething |
| **공식 웹사이트** | https://dotheth.ing |
| 설치 스크립트 | https://dotheth.ing/dtt.sh |
| 모델 기본값 파일 | https://dotheth.ing/models.txt |
| Notte (브라우저 프레임워크 포크) | https://github.com/fluffypony/notte |
| SearXNG (메타검색 엔진) | https://github.com/searxng/searxng |
| Peekaboo (macOS 화면 제어) | https://github.com/openclaw/Peekaboo |
| OpenRouter (API 키 발급) | https://openrouter.ai/keys |
| AgentMail (이메일 기능) | https://agentmail.to |
| 2Captcha (캡차 해결, 선택) | https://2captcha.com |

---

## 📑 목차

1. [이게 뭐야?](#1-이게-뭐야)
2. [저장소 구조 분석](#2-저장소-구조-분석)
3. [설치 및 사용법](#3-설치-및-사용법)
4. [플러그인? 스킬? MCP?](#4-플러그인-스킬-mcp)
5. [API 토큰과 비용](#5-api-토큰과-비용)
6. [왜 주목받을까](#6-왜-주목받을까)
7. [로컬 에이전트 학습 가이드](#7-로컬-에이전트-학습-가이드)
8. [수익화 전략](#8-수익화-전략)
9. [React / PHP로 만들 수 있을까](#9-react--php로-만들-수-있을까)
10. [법적 리스크 체크리스트](#10-법적-리스크-체크리스트)

---

## 1. 이게 뭐야?

**한 줄 요약: "심부름 시키면 혼자 다 해오는 AI 알바생"을 내 컴퓨터에 설치하는 것.**

터미널에 자연어로 작업을 던지면, 에이전트가 스스로:

1. 계획을 세우고 📋
2. 웹을 검색하고 🔎
3. 진짜 브라우저로 페이지를 읽고 🌐
4. 파일을 쓰고 셸 명령을 실행하고 ⚙️
5. **일이 끝날 때까지** 계속 작업 ✅

```bash
dtt "2025년 최대 데이터 유출 사고 10개 조사해서 원인과 타임라인을 마크다운 리포트로 써줘"
```

### 챗GPT와의 결정적 차이

| | 일반 AI (챗GPT 등) | dothething |
|---|---|---|
| 비유 | 📞 전화 상담원 | 🏃 직접 뛰는 알바생 |
| 하는 일 | "이렇게 하세요" 알려줌 | **실제로 실행함** |
| 파일/셸 | ❌ | ✅ |
| 지속성 | 한 번 답하고 끝 | **끝날 때까지 물고 늘어짐** |

### 만든 사람

- **Riccardo Spagni** (`fluffypony`) — 모네로(Monero) 프로젝트 전 리드 개발자
- 라이선스: **BSD 3-Clause** (상업적 이용 가능)

---

## 2. 저장소 구조 분석

### 파일 목록

| 파일 | 크기 | 정체 |
|---|---|---|
| **`dtt.sh`** | **559KB / 11,818줄** | ⭐ 프로그램 본체 전부 |
| `README.md` | 28KB / 372줄 | 사용 설명서 |
| `index.html` | 40KB / 912줄 | 공식 홈페이지 |
| `wrangler.jsonc` | - | Cloudflare Workers 배포 설정 |
| `og_image.jpg` | 167KB | 링크 공유용 썸네일 |
| `llms.txt` | 5KB | **AI가 읽으라고 만든 요약본** |
| `models.txt` | 1KB | 기본 모델 목록 (하루 1회 갱신) |
| `VERSION` | `3.0.12` | 현재 버전 |
| `version_up.sh` | - | 버전 업 + 커밋 + 푸시 자동화 |
| `tests/` | 948줄 | 브라우저 세션/CLI 테스트 |
| `CLAUDE.md` | - | 포크에서 추가된 Claude Code 설정 |

### 🤯 핵심: 파일 "하나"에 전부 들어있다

```bash
# dtt.sh 696번째 줄
cat > "$BASE/agent.py" << 'PYTHON_AGENT'
   ... 여기에 파이썬 에이전트 약 10,800줄이 통째로 ...
PYTHON_AGENT
```

**bash 껍데기 안에 Python 프로그램 전체가 문자열로 박혀있는 구조.**

> 🍱 도시락 비유: 밥, 반찬, 국, 젓가락까지 한 통에. 뚜껑 열면(실행하면) 알아서 세팅됨.

- **앞 ~700줄 (bash)**: 부트스트랩
  - `dtt_update()` (25줄) — 6시간마다 자동 업데이트 체크
  - `dtt_install()` (84줄) — `~/.local/bin/dtt` 설치 + PATH 등록
  - Python venv / SearXNG / Notte 브라우저 자동 설치
- **뒤 ~11,000줄 (Python)**: 두뇌
  - `TOOLS = [...]` (4164줄~) — **툴 60개 정의**
  - `SYSTEM_PROMPT` (5406줄~) — 시스템 프롬프트 (약 700줄)
  - `class Agent` (6167줄~) — 에이전트 루프 심장부
  - `class Browser` (1938줄), `class SearXNG` (1712줄)
  - `class CostTracker` (2951줄), `class SkillManager` (3316줄), `class MCPManager` (3381줄)
  - `class NotteSerpBridge` (2743줄) — 검색 차단 우회

### 그래서 설치가 한 줄

```bash
curl -fsSL dotheth.ing/dtt.sh | bash -s -- --install
```

의존성 충돌도, 도커도, 환경변수 20개도 없음.

---

## 3. 설치 및 사용법

### 설치

```bash
# 방법 A — 인터넷에서 원본 받기
curl -fsSL dotheth.ing/dtt.sh | bash -s -- --install

# 방법 B — 이미 클론한 저장소에서 (추천)
cd ~/dothething
./dtt.sh --install
```

**설치 시 실제로 일어나는 일** (`dtt_install()` 분석):

1. `~/.local/bin/dtt`에 스크립트 복사
2. 권한 `0755` 설정 (실행 가능하게)
3. **PATH 자동 등록** — zsh면 `~/.zshrc`, bash면 `~/.bashrc`에 한 줄 추가

⚠️ 등록 후 **터미널을 껐다 켜야** 적용됩니다.

### 첫 실행 (2~5분 소요)

```bash
dtt q "안녕 테스트야"
```

뒤에서 이런 걸 설치합니다:
- Python 가상환경 (`~/.dtt/cache/venv`)
- SearXNG (검색 엔진)
- Notte + Camoufox (지문 위장 브라우저)

그리고 **API 키를 물어봄** → `~/.dtt/env`에 저장 (권한 `0600`, 본인만 읽기 가능)

두 번째 실행부터는 바로 시작됩니다.

### 3가지 모드

```bash
# 🟢 quick — 제일 싸고 빠름, 한 방에 끝냄
dtt q "지금 비트코인 가격 알려줘"

# 🔵 normal (기본값) — 대부분 이걸로 충분
dtt "이 폴더의 코드 구조 분석해서 report.md로 정리해줘"

# 🔴 advanced — 제일 똑똑, 제일 비쌈
dtt --advanced "이 코드베이스 동시성 버그 찾아서 수정안 제시해줘"
```

### 꼭 알아야 할 옵션

| 옵션 | 뜻 | 중요도 |
|---|---|---|
| `--max-cost 2.00` | **$2까지만 쓰고 멈춤** (지갑 방어) | ⭐⭐⭐ 필수 |
| `--cwd ~/작업폴더` | 이 폴더에서만 작업 (안전) | ⭐⭐⭐ 필수 |
| `--resume <ID>` | 중단된 작업 이어하기 | ⭐⭐ |
| `--pipe` | 결과만 출력 (파이프라인 연결용) | ⭐⭐ |
| `--notify-desktop` | 끝나면 데스크톱 알림 | ⭐ |
| `--notify-email <주소>` | 끝나면 이메일 알림 | ⭐ |
| `--orchestrator` | 에이전트 여러 개 동시 실행 UI | ⭐ |
| `--browsermcp` | MCP 서버 모드로 실행 | ⭐⭐ |

### 💎 추천하는 안전한 첫 실행

```bash
mkdir ~/dtt-테스트 && cd ~/dtt-테스트
dtt --max-cost 0.50 --cwd . --notify-desktop \
    "오늘 서울 날씨 검색해서 weather.md로 저장해줘"
```

**돈 한도 + 작업 폴더 제한** — 이 두 개는 습관으로 만들기.

### 주요 폴더

| 위치 | 내용 |
|---|---|
| `~/.dtt/env` | API 키 저장소 (지우면 다시 물어봄) |
| `~/.dtt/threads/` | 작업 기록 (여기 ID로 `--resume`) |
| `~/.dtt/skills/<이름>/SKILL.md` | 사용자 정의 스킬 |
| `~/.dtt/mcp.json` | MCP 서버 설정 |
| `~/.dtt/browser-sessions/<이름>/profile/` | 브라우저 로그인 세션 |
| `/tmp/dothething/` | 임시 작업장 (재부팅 시 삭제됨, 정상) |

### 요구사항

- macOS 또는 Linux (**윈도우는 WSL 필요**)
- Python 3.11+, Git
- OpenRouter API 키
- 검색 브라우저 세션당 약 500MB 메모리 (기본 4개 = 약 2GB)

---

## 4. 플러그인? 스킬? MCP?

**정답: 셋 다 아님 — "독립 프로그램"입니다.**

다만 그 셋을 **전부 다룹니다**:

```
        ┌─────────────────────────────┐
        │   dothething (독립 프로그램)  │  ← 본체, 혼자 돌아감
        └─────────────────────────────┘
            ↓ 먹는 것          ↑ 되어주는 것
    ┌───────────────────┐   ┌──────────────────┐
    │ 🍽️ Skills 소비     │   │ 🔌 MCP 서버가 됨  │
    │ 🍽️ MCP 서버 소비   │   │   (다른 AI에게)   │
    └───────────────────┘   └──────────────────┘
```

### ① 소비하는 쪽

**스킬(Skills)**: `~/.dtt/skills/<이름>/SKILL.md`
→ **Claude Code와 완전히 같은 규격!** 기존 스킬을 그대로 복사해도 작동합니다.

```yaml
---
name: my-skill
description: 무엇을 하는지, 언제 쓰는지
allowed-tools: [Read, Write, Edit]   # 선택
disable-model-invocation: true       # 선택
---

스킬 지시사항...
```

- 이름과 설명만 프롬프트에 상주 → 스킬 수백 개여도 비용 거의 0
- 작업이 맞으면 `use_skill`로 전문을 읽어옴
- `allowed-tools`가 있으면 에이전트 본인 컨텍스트에서, 없으면 격리된 워커 모델 서브태스크로 실행

**MCP 서버**: `~/.dtt/mcp.json` (Claude Code와 같은 형식)

### ② 공급하는 쪽 — 가장 실용적인 활용

```bash
dtt --browsermcp
```

MCP 서버로 변신해서 다른 AI에게 툴 5개를 제공합니다:

| 툴 | 기능 |
|---|---|
| `dtt_search` | 봇 차단을 뚫는 웹 검색 |
| `dtt_fetch` | 페이지 내용 가져오기 |
| `dtt_browser` | 브라우저 조종 |
| `dtt_browser_session` | 로그인 세션 관리 |
| `dtt_browser_agent` | 복잡한 다단계 자동화 |

Claude Code 설정 예시:

```json
{
  "mcpServers": {
    "dtt-browser": {
      "command": "/Users/you/.local/bin/dtt",
      "args": ["--browsermcp", "--browser-session", "work"]
    }
  }
}
```

> 💰 **꿀팁**: `dtt_browser_agent`를 제외하면 **API 키 없이도 작동**합니다.
> 검색/페이지읽기/브라우저조작은 AI 원가가 0원.

### 결론

> **dothething = Claude Code의 "사촌".**
> 경쟁자이면서, 동시에 브라우저 능력을 빌려줄 수 있는 친구.

---

## 5. API 토큰과 비용

### 필수 🔴 — 딱 하나

**OpenRouter API 키** → https://openrouter.ai/keys

> 🏦 OpenRouter는 **"AI 모델 편의점"**. 원래 OpenAI/Anthropic/Google에 따로 결제해야 하는데,
> 여기 한 곳에 충전하면 모든 모델을 쓸 수 있습니다.

- **선불 충전 방식** (카드 등록 없이 가능 → 과금 폭탄 방지)
- 시작: **$5~10**이면 충분히 테스트 가능

### 선택 🟡 — 없어도 작동

| 환경변수 | 언제 필요 |
|---|---|
| `TWOCAPTCHA_API_KEY` | 캡차 자동 해결 |
| `AGENTMAIL_API_KEY` | 이메일 송수신 |
| `SERPER_API_KEY` | 검색 품질 향상 |

### 모델 구성 — 회사 조직도 같은 구조

| 역할 | normal (기본) | advanced |
|---|---|---|
| 🧑‍💼 **메인** (지휘) | DeepSeek Flash | Claude Fable |
| 🧑‍💻 **워커** (잡일/요약) | Gemini Flash | Gemini Flash |
| 🌐 **브라우저** | Claude Sonnet | Claude Fable |
| 👨‍🏫 **오라클** (막혔을 때만) | GPT-5.6 Terra | Astra |

quick 모드(`dtt q`)는 Claude Opus Fast 원샷, 오라클 없음.

**비싼 모델을 꼭 필요할 때만 부르는 구조** → 비용 대폭 절감.

모델 기본값은 https://dotheth.ing/models.txt 에서 하루 1회 가져오므로,
**프로그램 업데이트 없이 모델을 갈아탈 수 있습니다.**

역할별 덮어쓰기:
```bash
dtt --model oracle=x-ai/grok-5 "..."
dtt --model worker=google/gemini-flash-lite-latest "..."
dtt --model anthropic/claude-opus-5 "..."        # 맨 슬러그는 main
```
영구 설정은 `~/.dtt/env`의 `DTT_MODEL_MAIN` / `_WORKER` / `_ORACLE` / `_BROWSER`.

### 💸 비용 관리 3종 세트

1. **`--max-cost 2.00`** — 한도 도달 시 체크포인트 저장 후 중단 (`--resume`으로 재개)
2. **실시간 비용 표시** — `class CostTracker`가 계속 추적
3. **프롬프트 캐싱** — 긴 작업일수록 자동으로 비용 절감

---

## 6. 왜 주목받을까

> ⚠️ 원본 저장소의 실제 스타 수는 확인하지 않았습니다. 아래는 **코드를 직접 읽고 분석한 "주목받을 만한 이유"** 입니다.

### 🥇 만든 사람이 유명인
모네로 전 리드 개발자. 초기 확산의 절반은 이걸로 설명됩니다.

### 🥈 "파일 하나"라는 컨셉
```
❌ 보통: Docker 설치 → 이미지 빌드 → 환경변수 20개 → 에러 → 구글링...
✅ DTT: curl 한 줄. 끝.
```
**"Zero dependency, single file"** — 개발자 커뮤니티가 환장하는 컨셉.

### 🥉 진짜 어려운 문제를 영리하게 풀었음

**문제**: 구글이 봇을 차단해서, 일반 검색 라이브러리로는 *"자바스크립트를 켜주세요"* 껍데기만 받아옵니다. **결과가 0개인데 에러도 안 납니다.** 조용히 망하는 상황.

**해결**: 검색을 **진짜 브라우저(Camoufox)** 안에서 실행. 여기서 영리한 지점은:

> URL 생성과 결과 파싱은 **SearXNG 원본 코드에 그대로 맡기고**, 네트워크 요청만 브라우저로 교체.
> → 구글이 화면 구조를 바꿔도 **SearXNG가 매주 업데이트되면 자동으로 고쳐짐.**

**두 개의 레인**:
- **브라우저 레인**: Google, Scholar, Bing, DDG, Brave, Startpage, Yep, Yahoo
- **다이렉트 레인**: OpenAlex, Crossref, PubMed, arXiv, OpenAIRE, Mwmbl, Wikipedia
  → **가중치 2.0~3.0으로 일반 웹 결과보다 우선 배치** (리서치 품질의 핵심)

### 🏅 시대를 잘 탔음
2025~2026년 AI 에이전트 대유행기. 대부분 비싼 구독제 + 클라우드인데,
이건 **로컬 + 오픈소스 + 모델 자유 선택**.

### 🎖️ 코드가 "읽힘"
`version_up.sh` 주석처럼, **왜 그렇게 했는지**를 설명하는 주석이 전체에 깔려 있습니다.

---

## 7. 로컬 에이전트 학습 가이드

### 왜 최고의 교재인가

```
❌ 보통 오픈소스: src/agents/base/core/handlers/tools/... 파일 300개 미로
✅ dothething:    dtt.sh 하나를 위에서 아래로 쭉 읽으면 끝
```

**"AI 에이전트 해부학 교과서"** 입니다.

### 🗺️ 읽기 로드맵 (순서대로)

| 순서 | 위치 | 배우는 것 | 난이도 |
|---|---|---|---|
| 1 | **`TOOLS = [...]`** (4164줄~) | **툴 설계법.** AI에게 "이럴 때 써"를 어떻게 설명하는지, 60개 예시 | ⭐ |
| 2 | **`SYSTEM_PROMPT`** (5406줄~) | **프롬프트 엔지니어링 실전.** 약 700줄, "Turn 4: run_command(...)" 같은 시나리오 예시 포함 | ⭐ |
| 3 | **`class Agent`** (6167줄~) | **에이전트 루프.** `DISPATCH` 딕셔너리로 툴 이름 → 메서드 연결 | ⭐⭐ |
| 4 | `class CostTracker` (2951줄) | 비용 추적 + 캐싱 | ⭐⭐ |
| 5 | `SkillManager`/`MCPManager` (3316/3381줄) | 스킬·MCP 연결 | ⭐⭐ |
| 6 | `class NotteSerpBridge` (2743줄) | 검색 차단 우회 | ⭐⭐⭐ |

### 💡 훔칠 만한 설계 아이디어 BEST 4

#### ① `result_mode` — 컨텍스트 절약
모든 툴의 **필수 파라미터**입니다:
```python
"required": ["path", "result_mode"]
```
- `"raw"` = 원본 그대로
- `"함수 이름만 뽑아줘"` = **싼 워커 모델이 먼저 요약**해서 메인 에이전트에 전달

→ 긴 작업에서 컨텍스트 폭발을 막는 핵심 기법.

#### ② `exec_order` — 병렬 실행 스테이징
툴 7개에 `1,1,2,3,3,3,4` 번호를 붙여 한 번에 던지면:
```
[1,1] 동시 실행 → [2] → [3,3,3] 동시 실행 → [4]
```
"폴더 만들고 → 파일 3개 쓰고 → 목록 보기"가 왕복 1번에 끝납니다.
한 스테이지가 전부 실패하면 이후 스테이지는 건너뛰어 에이전트가 방향을 고칠 수 있게 합니다.

#### ③ 3단 모델 구조
팀장(메인) / 알바(워커) / 컨설턴트(오라클). 비싼 모델은 막혔을 때만 호출.

#### ④ SHA-256 스테일 체크
```
read_file  → 내용 + SHA-256 해시 반환
edit_file  → 그 해시 검증 → 안 맞으면 수정 거부
```
= **"내가 읽은 이후 파일이 바뀌었으면 수정하지 않음"** — 실무 사고 방지 디테일.

### 📚 추천 학습 코스

```
1주차: TOOLS 배열 읽기 → 툴 3개짜리 미니 에이전트 만들어보기
2주차: SYSTEM_PROMPT 읽기 → 내 프롬프트에 적용
3주차: Agent 루프 읽기 → exec_order 흉내내기
```

### 전체 툴 목록 (45+)

| 분류 | 툴 |
|---|---|
| **파일** | `read_file`, `write_file`, `edit_file`, `batch_read`, `diff_files` |
| **시스템** | `run_command`, `shell_session`, `run_code`, `glob`, `list_dir`, `search_file`, `clipboard_copy/paste`, `computer_use`(macOS), `request_user_input` |
| **웹** | `search_web`, `fetch_page`, `browser_agent`, `browser_session`, `http_request` |
| **분석** | `think`, `oracle`, `delegate`, `analyze_data`, `analyze_image`, `batch_process` |
| **상태** | `notes_add`, `notes_read`, `plan_create`, `plan_update` |
| **설정** | `manage_config`, `manage_skill`, `manage_mcp` |
| **이메일** | `email_auth`, `email_list`, `email_read`, `email_send`, `email_wait_for_message` 외 |
| **확장** | `use_skill`, MCP 툴 |

> 참고: `read_file`은 **PDF, DOCX, XLSX, PPTX, CSV를 자동 파싱**합니다.

---

## 8. 수익화 전략

### 🧊 먼저, 냉정한 현실 체크

**❌ 착각: "좋은 오픈소스니까 팔면 되겠지?"**
원본이 무료 공개입니다. `curl` 한 줄이면 누구나 공짜.
→ **"DTT 자체"는 상품이 될 수 없습니다.**

**⭕ 진실: 사람들이 돈 내는 3가지 이유**

| 이유 | 설명 |
|---|---|
| 😰 "설치/운영을 못 하겠어" | 터미널 무서운 사람 = 세상의 95% |
| ⏰ "내 시간이 더 비싸" | 직접 3시간 vs 맡기고 3만원 |
| 🎯 "내 문제를 정확히 풀어줘" | 만능 도구보다 **내 업무 전용** |

> 🔑 **핵심: "DTT를 파는 게 아니라, DTT로 푼 문제를 판다."**

### 📜 라이선스 — 사업 관점 해석

**BSD 3-Clause**는 오픈소스 중 가장 관대한 축입니다.

| 할 수 있는 것 | 가능 |
|---|---|
| 💰 돈 받고 팔기 | ✅ |
| 🔒 내가 고친 코드 비공개 유지 | ✅ (GPL이면 공개 의무) |
| 🏢 회사 제품에 넣기 | ✅ |
| 🎨 브랜드 바꾸기 | ✅ |

**지켜야 할 3가지**:
1. 저작권 문구(`Copyright (c) 2026, Riccardo Spagni`) + 라이선스 전문 포함
2. 바이너리 배포 시에도 문서/약관에 라이선스 포함
3. ❌ **원작자 이름으로 홍보 금지**
   - ❌ "모네로 개발자가 만든 엔진 탑재!"
   - ✅ "오픈소스 기반으로 제작"

> SaaS(웹서비스)는 "배포"가 아니라서 의무가 더 가볍지만, 표기해두는 게 안전합니다.

---

### 💸 【가장 중요】 원가 구조

**아이디어보다 이게 먼저입니다. 여기서 대부분 망합니다.**

#### 🚨 최대 함정: "무제한 정액제"

```
❌ "월 29,000원 무제한!"
   → 헤비유저 1명이 월 50만원어치 토큰 사용
   → 적자 💀
```

일반 소프트웨어는 원가가 0에 가깝지만, **AI는 쓸 때마다 진짜 돈이 나갑니다.** 완전히 다른 게임.

#### ✅ `--max-cost`가 사업적 최대 무기

```bash
dtt --max-cost 0.50 "..."
```

> 🛡️ 이건 안전장치가 아니라 **"원가 상한선"** 입니다.
> 작업 1건당 원가가 **수학적으로 확정** → 구조적으로 적자가 불가능.

#### `--pipe` 종료 코드 = 비즈니스 로직

| 코드 | 의미 | 사업 로직 |
|---|---|---|
| `0` | 완료 | ✅ 크레딧 차감, 결과 전달 |
| `2` | 부분 완료 | ⚠️ 절반 차감 or 재시도 |
| `1` | 실패 | 💯 **전액 환불** |

**"실패하면 돈 안 받습니다"** — 그대로 마케팅 문구가 됩니다.

#### 가격 정책 비교

| 방식 | 장점 | 위험 | 추천 |
|---|---|---|---|
| 💳 크레딧 선불제 | 원가 초과 불가 | 심리적 진입장벽 | ⭐⭐⭐⭐⭐ |
| 📦 정액 + 사용량 한도 | 예측 가능한 매출 | 한도 설계 실수 | ⭐⭐⭐⭐ |
| ♾️ 무제한 정액 | 팔기 쉬움 | **적자 폭탄** | ⭐ |

**추천 구조**:
```
🆓 무료체험: 3건
💳 크레딧: 10,000원 = 20건 (건당 500원)
📅 정기구독: 월 39,000원 = 100건 + 초과분 크레딧
```

**마진 공식** (실제 측정 후 적용):
```
판매가 = (평균 원가 × 3~5배) + 서버비 + 결제수수료(약 3%)
```
> 2배 이하는 위험합니다. AI 원가 외에 서버비/실패 재시도/고객응대/인건비가 들어갑니다.

**원가 측정법**:
```bash
# 실제 팔 작업을 10번 돌려 평균 내기 — 실행 중 비용이 실시간 표시됨
dtt --max-cost 1.00 --pipe "진짜 팔 작업 내용"
```

---

### 🏆 아이디어 — 5단계 티어

#### 🥇 TIER 1 — 서비스형 (오늘 당장 가능, 투자 0원)

**1-A. AI 자동화 구축 대행**

회사/개인 PC에 DTT 세팅 + 업무 맞춤 튜닝 + 사용법 교육.

- **왜 팔리나**: 모든 회사가 "AI 도입해야 하는데..." 상태 / 터미널 아는 직원이 없음 / **로컬 실행 = 데이터가 밖으로 안 나감** → 보안 민감 업종(법무·의료·금융·제조)에 강력

| 패키지 | 내용 | 가격대 |
|---|---|---|
| 🌱 기본 | 설치 + 기본 세팅 + 1시간 교육 | 30~50만원 |
| 🌿 표준 | + 전용 스킬 3개 제작 | 100~200만원 |
| 🌳 프리미엄 | + 월 유지보수 | 월 30~50만원 |

- ✅ 재고 0, 서버비 0, 리스크 0
- ❌ 시간이 곧 한계 (확장 어려움)
- 💎 **하지만 최고의 시작점**: 고객을 만나며 "진짜 뭐가 필요한지"를 배움 → Tier 2의 재료. **시장조사를 돈 받으면서** 하는 셈.

**1-B. 리서치 / 데이터 대행**

- 판매 예시: "경쟁사 20곳 가격·기능 비교표", "논문 50편 요약 리포트"(PubMed/arXiv/OpenAlex 직접 조회로 품질 우수), "업계 월간 동향 리포트"
- 판매처: 크몽, 숨고, 탈잉, Fiverr, Upwork
- **수익률**:
  ```
  판매가 10만원 / 원가 --max-cost 1.00 (약 1,400원) / 내 시간 10분
  → 마진 약 98%
  ```
- ❌ 일회성 매출
- 💡 **해결**: "매월 업데이트 리포트 구독 월 5만원"으로 반복 매출화

---

#### 🥈 TIER 2 — 제품형 SaaS (2~3개월)

**핵심 전략: "만능" 버리고 "하나만"**
```
❌ "AI 에이전트 서비스"      → 대기업과 정면승부, 패배
✅ "쇼핑몰 경쟁사 가격 추적"  → 니치 1등, 승리
```

**아이템 3개**

| # | 아이템 | 타겟 | 가격 | DTT 활용 |
|---|---|---|---|---|
| ① | 🏷️ 경쟁사 가격 모니터링<br>(매일 새벽 확인 → 아침 메일) | 쇼핑몰 운영자, 브랜드사 | 월 3~10만원 | `fetch_page` + `--notify-email` |
| ② | 📰 브랜드/키워드 모니터링<br>(언급 감지 + 감성 분석) | 마케팅팀, PR 대행사 | 월 5~20만원 | **검색 차단 우회**가 차별점 |
| ③ | 📑 문서 자동 처리<br>(계약서 → 핵심 조항 추출) | 중소기업 총무/법무 | 건당 or 월정액 | `read_file`이 PDF/DOCX/XLSX 이미 지원 |

**최소 구조 (MVP)**
```
[웹 화면]  ← React 또는 PHP
    ↓ 작업 등록
[작업 큐]  ← DB 테이블 하나면 충분
    ↓
[워커]     ← dtt --pipe --max-cost 0.50 실행
    ↓ 종료코드로 성공/실패 판단
[결과 저장 → 메일 발송]
```

PHP에서 워커 호출:
```php
$작업 = escapeshellarg($사용자작업);   // ⚠️ 필수!
$결과 = shell_exec("dtt --pipe --max-cost 0.50 --cwd /작업폴더 $작업");
// 종료코드: 0=성공, 2=부분, 1=실패(환불)
```

> 🚨 **보안**: `escapeshellarg()` 없이 사용자 입력을 넣으면 **명령어 주입 공격**을 당합니다.
> 워커는 반드시 **격리 환경(도커/전용 계정)** 에서 실행하세요.

---

#### 🥉 TIER 3 — 자산형 (복리로 쌓임)

**3-A. 스킬팩 / 템플릿 판매**

DTT 스킬은 그냥 `SKILL.md` 텍스트 파일 → **"잘 쓴 프롬프트 = 상품"**

- 📊 마케터팩 / 💻 개발자팩 / 🇰🇷 **한국 특화팩**
- 가격: 팩당 2~5만원, 전체 구독 월 1만원
- 🔥 **핵심 기회**: DTT 스킬 규격이 **Claude Code와 동일** → **한 번 만들어 두 시장에 판매**

**3-B. 콘텐츠 / 교육**

소스가 파일 1개라 해설 콘텐츠 제작에 최적.

| 형태 | 내용 | 수익 |
|---|---|---|
| 🎥 유튜브 | "AI 에이전트 소스 한 줄씩 뜯어보기" | 광고 + 유입 |
| 📚 유료강의 | "나만의 로컬 AI 에이전트 만들기" | 5~15만원 |
| ✍️ 블로그 | 기술 시리즈 | 신뢰도 → Tier 1 고객 유입 |
| 📕 전자책 | "에이전트 설계 패턴" | 1~3만원 |

> 한국어 에이전트 심화 콘텐츠가 거의 없어 선점 기회가 큽니다.
> 직접 수익보다 **"신뢰 자산"** 이 더 큼 → Tier 1 고객이 알아서 들어오는 선순환.

**3-C. MCP 브라우저 서비스**

`--browsermcp`를 "AI에 붙이는 브라우저 팔"로 포장.
- 타겟: Claude Code / Cursor 사용 개발자
- 셀링: "당신의 AI가 로그인 필요한 사이트도 읽게 만드세요"
- 🎁 **`dtt_browser_agent` 제외 시 AI 원가 0원** (서버비만)

---

#### 🏅 TIER 4 — 조합 기술 (고급)

**오케스트레이터 활용 — "AI 팀 대여"**
```bash
dtt --orchestrator   # 에이전트 최대 16개 동시 실행
```
"AI 인턴 16명이 동시에 조사합니다" 포지션. 대량 리서치 B2B 프로젝트 건당 100~500만원.

**이메일 에이전트 — 24시간 응대**
`email_wait_for_message`로 답장 대기까지 가능 → 문의 자동 분류/1차 응대.

---

### 🚫 하지 말아야 할 것

- ❌ **그대로 재포장해서 팔기** — 원본이 무료 공개인데 살 이유가 없음
- ❌ **약관 위반 크롤링 서비스** — 법적 리스크 과다
- ❌ **"만능 AI 에이전트"** 포지셔닝 — 대기업과 정면승부

---

### 🗓️ 90일 실행 플랜

**0~14일: 검증 + 측정**
```
✅ DTT 설치하고 20번 이상 직접 사용
✅ 실제로 팔 작업 10건 돌려 원가 측정  ← 핵심
✅ 크몽/숨고에 리서치 대행 상품 1개 등록
🎯 목표: 첫 1만원 — 금액이 아니라 "팔린다"는 증명
```

**15~45일: Tier 1 굴리기**
```
✅ 리서치 대행 10건 처리 → 어떤 요청이 많은지 파악
✅ 블로그/유튜브 콘텐츠 3개 → 유입 만들기
✅ 구축 대행 1건 수주 시도
🎯 목표: 월 100만원 + "가장 자주 오는 요청" 발견
```

**46~90일: Tier 2 MVP**
```
✅ 가장 많이 온 요청 1개를 SaaS로 제품화
✅ 결제 연동 (토스페이먼츠 / 포트원)
✅ Tier 1 고객을 첫 SaaS 유저로 전환
🎯 목표: 반복 매출(MRR) 시작
```

> 💡 **왜 이 순서인가**: 대부분 SaaS부터 만들다 망합니다. 6개월 만들었는데 아무도 안 씀.
> Tier 1으로 **"돈 내는 사람이 진짜 원하는 것"** 을 먼저 배우고 제품화하는 게 훨씬 안전합니다.

---

### 🎯 딱 하나만 고른다면

> ## 🏆 리서치 대행(Tier 1-B)으로 시작 → 가장 많이 온 요청을 SaaS로 전환

| 이유 | 설명 |
|---|---|
| 💵 투자금 0원 | 실패해도 잃는 건 시간뿐 |
| ⚡ 이번 주 첫 매출 | 동기부여가 가장 중요 |
| 🔍 돈 받으며 시장조사 | 고객이 직접 알려줌 |
| 🎁 만들 게 없음 | DTT가 이미 다 됨 |
| 📈 자연스러운 확장 | 반복 요청 = SaaS 아이템 |

**오늘 당장 할 일 (30분)**
```bash
./dtt.sh --install

dtt --max-cost 1.00 --notify-desktop \
  "국내 노코드 툴 서비스 10개 조사해서, 가격/기능/타겟 비교표를
   markdown 표로 만들고 comparison.md로 저장해줘"

# → 결과물 보고 판단: "이거 10만원 받을 만한가?"
```
이 결과물이 그대로 판매 페이지 샘플이 됩니다.

> 💖 **기술은 이미 다 있습니다. 진짜 승부는 "누구의 어떤 문제를 풀 것인가"** 이고,
> 그건 코드가 아니라 사람을 만나며 알게 됩니다. 완벽하게 만들려 하지 말고, 일단 하나 팔아보세요.

---

## 9. React / PHP로 만들 수 있을까

**결론: React는 ❌, PHP는 △, 조합하면 ⭕**

### ⚛️ React — 본체로는 불가능

React는 **브라우저 안에서 도는 UI 라이브러리**입니다. 에이전트가 하는 일과 안 맞습니다.

| 에이전트의 일 | 브라우저에서 가능? |
|---|---|
| 파일 읽고 쓰기 | ❌ 보안상 차단 |
| 셸 명령 실행 | ❌ 절대 불가 |
| API 키 보관 | ❌ F12로 다 보임 |
| 다른 사이트 긁기 | ❌ CORS 차단 |

> 🍽️ React는 **예쁜 접시**. 요리는 못 하지만 담아서 보여주는 건 최고.

**✅ 올바른 역할 = UI**
```
[React 화면] ←→ [Node.js 서버: 에이전트 본체] ←→ [모델 API]
  예쁜 채팅창          진짜 일하는 곳
```
Next.js면 한 프로젝트에 프론트 + API 라우트가 같이 들어갑니다.

### 🐘 PHP — 되긴 되지만 힘듦

원리상 문제는 없습니다(HTTP 요청 + JSON 파싱). 하지만:

| 문제 | 설명 |
|---|---|
| ⏰ 시간 제한 | 웹 요청은 보통 30초 컷. 에이전트는 30분씩 돔 |
| 🔀 병렬 처리 약함 | `exec_order`(툴 동시 실행) 흉내내기 어려움 |
| 🌐 브라우저 자동화 생태계 빈약 | Playwright/Puppeteer는 Node/Python 중심 |
| 🤖 AI 라이브러리 적음 | 대부분 Python/JS가 먼저 |

**✅ 올바른 역할 = 관제탑**
```
[PHP/Laravel: 회원가입, 결제, 대시보드, 작업 등록]
              ↓ 큐(Queue)에 작업 투입
[Python/Node 워커: 실제 에이전트 실행]  ← 오래 걸려도 OK
              ↓ 결과 DB 저장
[PHP: 결과 표시]
```
> 🏢 PHP는 **프론트 데스크**(접수/결제/안내), 실제 작업은 **작업장(워커)** 에서.

### 🧩 진실: 에이전트 "루프"는 아주 간단

```javascript
// AI 에이전트의 핵심은 사실상 이게 전부
let 대화기록 = [사용자_요청];

while (turn < 200) {
  const 응답 = await AI한테_물어보기(대화기록, 툴목록);

  if (응답.툴호출 없음) break;         // 할 일 없으면 종료

  for (const 툴 of 응답.툴호출) {
    const 결과 = 툴실행(툴.이름, 툴.인자);   // ← switch문일 뿐
    대화기록.push(결과);
  }
}
```

**딱 10줄.** `while` 루프 + API 호출 + `switch`문이 전부입니다.

### 😤 진짜 어려운 건 "주변부"

DTT가 11,818줄인 이유:

| 어려운 것 | 왜 |
|---|---|
| 🌐 브라우저 자동화 | 봇 차단 우회, 캡차, 로그인 유지 |
| 🔎 검색 품질 | 구글 차단 우회 |
| 📏 컨텍스트 관리 | 대화 길어지면 폭발 → 요약 전략 |
| 🔁 에러 복구 | 모델이 헛소리할 때 재시도 |
| 🔒 동시성/파일 락 | 병렬 수정 충돌 방지 |
| 💰 비용 추적 | 캐싱, 한도 관리 |

> 💡 **루프는 1시간이면 만듭니다. 나머지가 6개월입니다.**

### 🎯 추천

**"직접 만들며 배우고 싶다"**
```
🥇 백엔드: Python 또는 Node.js/TypeScript
🥈 프론트: React (Next.js면 한 번에)
🥉 PHP: 기존 PHP 서비스에 "붙이는 용도"
```

**"빨리 서비스를 만들고 싶다"**
```bash
# DTT를 그대로 백엔드로 쓰고 결과만 받기
dtt --pipe --max-cost 1.00 "작업내용" > 결과.md
```
PHP에서도 `shell_exec()` 한 줄로 호출 가능합니다(위 보안 주의사항 필독).

> 🛞 **바퀴를 다시 발명하지 말고, 잘 만든 바퀴를 굴립시다.**

---

## 10. 법적 리스크 체크리스트

브라우저 자동화로 사업할 때 **여기서 사고가 납니다.**

| 항목 | 체크 포인트 |
|---|---|
| 📜 **이용약관(ToS)** | 크롤링 금지 조항 확인. 대형 사이트는 대부분 있음 |
| 🤖 **robots.txt** | 법적 구속력은 약해도, 위반 사실이 불리한 정황이 됨 |
| 🔐 **개인정보보호법** | **개인정보 수집 절대 금지** (이름/연락처/이메일) |
| ⚖️ **저작권** | 기사 전문 복제 ❌ / 요약·링크 ⭕ |
| 💾 **DB권(저작권법)** | 타사 DB 상당 부분 복제는 별도 보호 권리 침해 가능 |
| 🏛️ **정보통신망법** | 로그인 우회, 권한 없는 영역 접근 = **형사처벌 대상** |
| 🖥️ **서버 부하** | 과도한 요청은 업무방해 소지. 속도 제한 필수 |

### ✅ 안전 존
- 공개된 가격/스펙 정보 (약관 허용 범위)
- **고객 본인 계정을 고객 동의하에 자동화** ← 가장 안전
- 공공 API, 학술 DB (OpenAlex/PubMed/arXiv — DTT가 이미 직접 지원)
- 자사 데이터 처리

### ❌ 위험 존
- 타 사이트 DB 통째로 복제 후 재판매
- 로그인 우회
- 개인정보 수집
- 캡차 대량 우회로 상업적 이득

> 🛡️ **안전 원칙: "고객이 직접 해도 되는 일을, 고객 대신 자동으로 해준다."**
> 이 선만 지키면 대부분 안전합니다.
> B2B 계약서에 **"데이터 수집 적법성은 고객 책임"** 조항을 넣는 것도 방법입니다.

---

## 📌 한 장 요약

| 질문 | 답 |
|---|---|
| **이게 뭐야?** | 터미널에서 혼자 일하는 자율 AI 에이전트. bash 파일 1개 |
| **설치** | `./dtt.sh --install` → 첫 실행 2~5분 → `--max-cost` 꼭 걸기 |
| **정체** | 독립 프로그램. 스킬·MCP를 *먹기도* 하고, MCP 서버가 *되기도* 함 |
| **API 키** | OpenRouter **하나만 필수** (선불 충전). 나머지는 선택 |
| **인기 이유** | 유명 개발자 + 파일 1개 + 검색 차단 우회 + 시대 타이밍 |
| **학습 가치** | **최고.** `TOOLS` → `SYSTEM_PROMPT` → `Agent` 순서로 읽기 |
| **수익화** | 구축 대행 🥇 → 니치 SaaS 🥈 → 한국 특화팩 🥉 (BSD라 상업 이용 OK) |
| **React/PHP** | React=UI, PHP=관제탑, 본체는 Python/Node. 또는 `--pipe`로 그냥 활용 |
| **주의** | `--max-cost` + `--cwd` 항상 / 크롤링 법적 리스크 확인 |

---

*이 문서는 저장소 소스를 직접 읽고 분석해 작성되었습니다.*
*분석 대상 버전: `VERSION` 3.0.12 / `dtt.sh` 11,818줄*
